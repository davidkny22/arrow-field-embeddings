[project]
name = "afe-viewer-server"
version = "0.1.0"
description = "AFE Viewer backend server"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "numpy>=1.24",
]

[project.scripts]
serve = "app.main:run"
